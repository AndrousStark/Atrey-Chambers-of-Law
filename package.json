{
  "name": "fullstack-website",
  "private": true,
  "workspaces": [
    "frontend-next"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend-next",
    "build": "npm run build --workspace frontend-next"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
