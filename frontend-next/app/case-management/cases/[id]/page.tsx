'use client';

// This page works in local dev (non-export mode).
// On GitHub Pages (static export), this route is removed and
// case detail is rendered via ?view=CASE_ID on the cases list page.
export { default } from '@/components/cms/cases/CaseDetailView';
